homeassistant:
  name: Day Mode DevContainer
  latitude: 48.8566
  longitude: 2.3522
  elevation: 0
  unit_system: metric
  country: FR
  time_zone: Europe/Paris

# Enable development mode
logger:
  default: info
  logs:
    homeassistant.components.lovelace: debug
    custom_components.day_mode: debug

# User interface - use YAML mode to load ui-lovelace.yaml
lovelace:
  mode: yaml
  resources:
    - url: /local/scheduler-card/scheduler-card.js
      type: module

# Scheduler integration
scheduler:
  # No specific config needed, component will auto-configure
